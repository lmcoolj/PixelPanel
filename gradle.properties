# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false
# Toolchain: build with JDK 25. By default Gradle uses the JDK that JAVA_HOME
# points at. To pin a specific install instead, set org.gradle.java.home to its
# path (machine-specific — keep it out of version control if you do).

# Fabric Properties
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.1.0
maven_group=com.pixelpanel
archives_base_name=pixelpanel

# Dependencies
fabric_api_version=0.152.2+26.2
